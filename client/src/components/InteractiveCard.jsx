/* import React, { useState, useCallback} from 'react';
import './InteractiveCard.css'; // import the CSS from src

function InteractiveCard() {
  const [isHovered, setIsHovered] = useState(false); //Declare a state variable 'isHovered' to track hover state, and 'setIsHovered' to update its value. Initially set to 'false'.

  return (
    <div
      className={'interacive-card ${isHovered ? 'hovered' : ''}'}
      onMouseEnter={() => setIsHovered(true)}
      onMouseLeave={() => setIsHovered(false)}
    >
      <img
        src="https://via.placeholder.com/150">
        alt="Example"
        cassName="card-image"
      />
        <h2>Interacive Card</h2>
        <button className="card-button">Click Me</button>
  </div>
  );
} */